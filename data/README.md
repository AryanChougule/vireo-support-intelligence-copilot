# Data placement

Copy the assessment CSV files into this directory, or pass their directory to the CLI:

```bash
python run_analysis.py --data-dir /path/to/assessment/files
```

Expected files: `*agents.csv`, `*customers.csv`, `*products.csv`, `*orders.csv`, and `*tickets.csv`.

Raw assessment data is intentionally not committed to the repository.
